<!-- res.users · view 436 · res.users.form.inherit · herda de 167 · form · prioridade 16 -->
<data>
  <!-- add state field in header -->
  <xpath expr="//header" position="inside">
    <button string="Send an Invitation Email" type="object" name="action_reset_password" context="{'create_user': 1}" invisible="state != 'new'"/>
    <field name="state" widget="statusbar"/>
  </xpath>
  <xpath expr="//div[@name='auth']" position="inside">
    <button string="Send Password Reset" type="object" name="action_reset_password" invisible="id == uid or state != 'active'" class="btn btn-secondary h-100 ms-2 my-auto"/>
    <widget name="auth_signup.button_reset_password_link" groups="base.group_erp_manager" invisible="id == uid"/>
  </xpath>
</data>