<!-- x_parametros_line_c498a · list combinada (via get_view) · view 616
     SOMENTE LEITURA: é o resultado das heranças, não existe como registro. O --update ignora este arquivo. -->
<list default_order="x_studio_sequence asc,id desc">
  <field name="x_studio_sequence" widget="handle"/>
  <field name="x_name"/>
</list>