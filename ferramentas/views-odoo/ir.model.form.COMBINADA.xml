<!-- ir.model · form combinada (via get_view) · view 47
     SOMENTE LEITURA: é o resultado das heranças, não existe como registro. O --update ignora este arquivo. -->
<form string="Model Description" duplicate="0">
  <header>
    <!-- used for override -->
  </header>
  <sheet>
    <div class="oe_title">
      <label for="name"/>
      <h1>
        <field name="name" placeholder="e.g. Sales Order"/>
      </h1>
    </div>
    <group>
      <field name="explanation" placeholder="Provide a verbose explanation of the model's purpose..."/>
    </group>
    <group>
      <group>
        <field name="model" readonly="id"/>
        <field name="order"/>
        <field name="abstract" readonly="id" invisible="1" column_invisible="1"/>
        <field name="transient" readonly="id" invisible="1" column_invisible="1"/>
        <field name="is_mail_thread" readonly="state != 'manual'" invisible="1" column_invisible="1"/>
        <field name="is_mail_activity" readonly="state != 'manual'" invisible="1" column_invisible="1"/>
        <field name="is_mail_blacklist" readonly="state != 'manual'" invisible="1" column_invisible="1"/>
      </group>
      <group>
        <field name="state" readonly="1" invisible="1" column_invisible="1"/>
        <field name="modules" invisible="1" column_invisible="1"/>
      </group>
    </group>
    <notebook>
      <page string="Fields" name="fields">
        <field name="field_id" context="{'default_model': model}">
          <list string="Fields Description">
            <field name="name"/>
            <field name="field_description"/>
            <field name="ttype" on_change="1"/>
            <field name="required"/>
            <field name="readonly"/>
            <field name="index" invisible="1" column_invisible="1"/>
            <field name="state" invisible="1" column_invisible="1"/>
          </list>
          <form string="Fields Description">
            <group>
              <group>
                <field name="name"/>
                <field name="field_description"/>
              </group>
              <group>
                <field name="ttype" on_change="1"/>
                <field name="help"/>
              </group>
            </group>
            <notebook>
              <page name="base" string="Properties">
                <group>
                  <group>
                    <field name="required"/>
                    <field name="readonly"/>
                    <field name="store" invisible="1" column_invisible="1"/>
                    <field name="index" invisible="1" column_invisible="1"/>
                    <field name="copied" invisible="1" column_invisible="1"/>
                    <field name="state" invisible="1"/>
                    <field name="tracking" readonly="state != 'manual'"/>
                    <field name="state" invisible="1" column_invisible="1"/>
                    <field name="modules" invisible="1" column_invisible="1"/>
                  </group>
                  <group>
                    <field name="translate" invisible="ttype not in ['char', 'text', 'html']" readonly="ttype not in ['char', 'text', 'html']"/>
                    <field name="size" invisible="1" readonly="ttype not in ['char', 'reference']" column_invisible="1"/>
                    <field name="relation" invisible="ttype not in ['many2one', 'one2many', 'many2many']" readonly="ttype not in ['many2one', 'one2many', 'many2many']" required="ttype in ['many2one', 'one2many', 'many2many']" on_change="1"/>
                    <field name="on_delete" invisible="1" readonly="ttype != 'many2one'" column_invisible="1"/>
                    <field name="relation_field" invisible="ttype != 'one2many' or related or compute" readonly="ttype != 'one2many'" required="ttype == 'one2many' and not related and not compute"/>
                    <field name="relation_table" invisible="1" readonly="ttype != 'many2many' or state != 'manual'" on_change="1" column_invisible="1"/>
                    <field name="column1" invisible="1" readonly="ttype != 'many2many' or state != 'manual'" column_invisible="1"/>
                    <field name="column2" invisible="1" readonly="ttype != 'many2many' or state != 'manual'" column_invisible="1"/>
                    <field name="domain" invisible="1" readonly="ttype not in ['many2one', 'one2many', 'many2many']" column_invisible="1"/>
                  </group>
                </group>
                <group invisible="ttype not in ['selection', 'reference']" readonly="ttype not in ['selection', 'reference']">
                  <field name="selection_ids">
                    <list editable="bottom">
                      <field name="sequence" widget="handle"/>
                      <field name="value"/>
                      <field name="name"/>
                    </list>
                  </field>
                </group>
              </page>
              <page name="compute" string="Compute" invisible="1" column_invisible="1">
                <group>
                  <field name="related" on_change="1"/>
                  <field name="depends" required="compute not in [False, '']"/>
                  <field name="compute" widget="code" options="{'mode': 'python'}" on_change="1"/>
                </group>
                <div>
                  <h3>
                    How to define a computed field
                  </h3>
                  <p>
                    Computed fields are defined with the fields
                    <strong>
                      Dependencies
                    </strong>
                    and
                    <strong>
                      Compute
                    </strong>
                    .
                  </p>
                  <p>
                    The field
                    <strong>
                      Dependencies
                    </strong>
                    lists the fields that the current field depends on. It is a comma-separated list of field names, like
                    <code>
                      name, size
                    </code>
                    . You can also refer to fields accessible through other relational fields, for instance
                    <code>
                      partner_id.company_id.name
                    </code>
                    .
                  </p>
                  <p>
                    The field
                    <strong>
                      Compute
                    </strong>
                    is the Python code to compute the value of the field on a set of records. The value of the field must be assigned to each record with a dictionary-like assignment.
                  </p>
                  <pre>
                    for record in self: record['size'] = len(record.name)
                  </pre>
                  <p>
                    The only predefined variables are
                  </p>
                  <ul>
                    <li>
                      <code>
                        self
                      </code>
                      (the set of records to compute)
                    </li>
                    <li>
                      <code>
                        datetime
                      </code>
                      (Python module)
                    </li>
                    <li>
                      <code>
                        dateutil
                      </code>
                      (Python module)
                    </li>
                    <li>
                      <code>
                        time
                      </code>
                      (Python module)
                    </li>
                  </ul>
                  <p>
                    Other features are accessible through
                    <code>
                      self
                    </code>
                    , like
                    <code>
                      self.env
                    </code>
                    , etc.
                  </p>
                </div>
              </page>
              <page name="groups" string="Access Rights">
                <field name="groups" can_create="True" can_write="True"/>
              </page>
              <page name="ai" string="AI" invisible="ttype in ('binary', 'one2many', 'reference')">
                <group>
                  <field name="ai" readonly="state == 'base'"/>
                  <field name="model" invisible="1"/>
                  <!-- Used by the `ai_prompt` widget -->
                  <field name="system_prompt" invisible="not ai" readonly="state == 'base'" widget="ai_prompt" options="{                                 'model_reference_field': 'model',                                 'field_reference_field': 'name',                                 'record_selector_relation_field': 'relation'                             }"/>
                </group>
              </page>
            </notebook>
          </form>
        </field>
        <button icon="fa-align-justify" name="2" string="Create a Menu" type="action" target="new" invisible="1" column_invisible="1"/>
      </page>
      <page string="Access Rights" name="access_rights">
        <field name="access_ids">
          <list string="Access Rights" editable="top" decoration-warning="not group_id and                                                          (perm_read or perm_write or                                                           perm_create or perm_unlink)">
            <field name="name"/>
            <field name="group_id" can_create="True" can_write="True"/>
            <field name="perm_read"/>
            <field name="perm_write"/>
            <field name="perm_create"/>
            <field name="perm_unlink"/>
          </list>
        </field>
      </page>
      <page string="Record Rules" name="record_rules">
        <field name="rule_ids">
          <list string="Record Rules" editable="top" decoration-info="not groups">
            <field name="name"/>
            <field name="groups" widget="many2many_tags" options="{'no_create':True}" can_create="True" can_write="True"/>
            <field name="domain_force"/>
            <field name="perm_read"/>
            <field name="perm_write"/>
            <field name="perm_create"/>
            <field name="perm_unlink"/>
          </list>
        </field>
      </page>
      <page string="Notes" name="notes" invisible="1" column_invisible="1">
        <separator string="Information"/>
        <field name="info"/>
      </page>
      <page string="Views" name="views" invisible="1" column_invisible="1">
        <field name="view_ids">
          <list string="Views">
            <field name="priority" string="Sequence" widget="handle"/>
            <field name="name"/>
            <field name="type"/>
            <field name="model"/>
            <field name="xml_id" invisible="1" column_invisible="1"/>
            <field name="inherit_id"/>
          </list>
        </field>
      </page>
    </notebook>
  </sheet>
</form>